services:
  chemediaho:
    image: gablilli/chemediaho:latest
    container_name: chemediaho
    ports:
      - "8001:8001"
    environment:
      FLASK_ENV: production
      STANDALONE_MODE: "true"  # Serve frontend + API from Flask
      # API_BASE: "https://your-openviva-api"  # openviva api endpoint when STANDALONE_MODE=false
      # API_KEY: "your-secret"  # Enable for API key protection
    volumes:
      # persiste la secret key fuori dal container
      - ./secret_key.txt:/app/secret_key.txt
    restart: always
